## 📋 **对方接收P2P模型分发的准备指南**

要让另一台电脑接收您切分好的测试模型，对方需要按以下步骤准备：

### 🔧 **1. 环境准备**

#### **必需软件**：
```bash
# 安装 Rust (1.70+)
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
source ~/.cargo/env

# 验证安装
rustc --version
cargo --version
```

#### **系统依赖**：
- **Windows**: Visual Studio Build Tools
- **macOS**: Xcode Command Line Tools  
- **Linux**: `build-essential` 包

### 📥 **2. 获取代码**

```bash
# 克隆仓库
git clone https://github.com/logos-42/williw.git
cd williw

# 切换到最新版本
git checkout master
git pull origin master
```

### 🚀 **3. 编译项目**

```bash
# 编译库和示例
cargo build --release --lib
cargo build --release --example p2p_model_distribution_demo

# 验证编译成功
cargo check --lib
```

### 🌐 **4. 网络准备**

#### **防火墙设置**：
- 开放端口范围：`0.0.0.0:0` (随机端口)
- 允许UDP/TCP出站连接
- 如果有路由器，可能需要端口转发

#### **网络连接**：
- 确保可以访问互联网（iroh需要中继服务器）
- 局域网内直接连接更佳

### 🎯 **5. 运行接收端**

#### **基本接收命令**：
```bash
# 启动接收端
cargo run --release --example p2p_model_distribution_demo -- receive

# 或者指定接收目录
cargo run --release --example p2p_model_distribution_demo -- receive --output-dir ./received_models
```

#### **高级接收选项**：
```bash
# 完整参数示例
cargo run --release --example p2p_model_distribution_demo -- receive \
  --output-dir ./received_models \
  --checksum-algorithm sha256 \
  --enable-resume \
  --max-concurrent-transfers 3
```

### 📝 **6. 获取您的节点信息**

您需要提供以下信息给对方：

#### **您的节点ID**：
```bash
# 在您的电脑上运行
cargo run --release --example p2p_model_distribution_demo -- full

# 查看输出中的节点ID，类似：
# 🔑 本地节点 ID: 12D3KooWExampleNodeIdHere...
```

#### **连接信息**：
- **节点ID**: 您的iroh端点ID
- **中继服务器**: 如果使用特定中继
- **直接地址**: 如果在同一局域网

### 🔗 **7. 建立连接**

#### **对方连接到您**：
```bash
# 对方使用您的节点ID连接
cargo run --release --example p2p_model_distribution_demo -- receive \
  --connect-to "12D3KooWYourNodeIdHere..."

# 或者在中继模式下
cargo run --release --example p2p_model_distribution_demo -- receive \
  --connect-to "12D3KooWYourNodeIdHere..." \
  --relay-url "https://relay.example.com"
```

### 📊 **8. 监控传输进度**

接收端会显示：
```
📥 接收模型文件...
📊 传输进度: 45% (速度: 2.3MB/s)
✅ 文件接收完成: model_part_1.bin
🔍 完整性检查: SHA256 验证通过
📁 保存到: ./received_models/
```

### ⚠️ **9. 常见问题解决**

#### **连接失败**：
```bash
# 检查网络连通性
ping your-computer-ip

# 尝试使用中继模式
--relay-url "https://relay.iroh.network"

# 检查防火墙设置
```

#### **传输中断**：
```bash
# 启用断点续传
--enable-resume

# 增加重试次数
--max-retries 5
```

### 🎯 **完整接收流程示例**

```bash
# 1. 对方准备环境
git clone https://github.com/logos-42/williw.git
cd williw
cargo build --release

# 2. 启动接收端（使用您的节点ID）
cargo run --release --example p2p_model_distribution_demo -- receive \
  --connect-to "12D3KooWYourNodeIdHere..." \
  --output-dir ./models

# 3. 等待传输完成
# 系统会自动显示进度和验证结果
```

### 📞 **10. 测试连接**

建议先进行小文件测试：
```bash
# 您发送测试文件
echo "test" > test.txt
cargo run --release --example p2p_model_distribution_demo -- send \
  --file test.txt \
  --peer-id "12D3KooWReceiverNodeId..."

# 对方接收
cargo run --release --example p2p_model_distribution_demo -- receive
```

**准备好这些步骤后，对方就可以成功接收您切分好的测试模型了！** 🚀
